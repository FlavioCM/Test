Please install node and npm if it's not already installed on your computer.

To install dependencies, enter this line of code into the command line:
	npm install request array-sort weekday console.table moment-timezone

After dependencies are installed, to run program locally, enter the following line into the command line:
	node FoodTruckFinder.js


